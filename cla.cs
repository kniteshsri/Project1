using System;

class CLA
{
	public static void Main(string[]Nitesh)
	{
		int n1,n2,sum;
		n1=Int32.Parse(Nitesh[0]);
		n2=Int32.Parse(Nitesh[1]);

		sum=n1+n2;
		Console.WriteLine("Sum is "+sum);
	}
}